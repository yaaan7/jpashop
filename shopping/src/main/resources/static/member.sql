CREATE TABLE member (
                        member_id BIGINT AUTO_INCREMENT PRIMARY KEY,
                        city VARCHAR(255),
                        street VARCHAR(255),
                        zipcode VARCHAR(20),
                        username VARCHAR(100)
);